<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8">
        <tittle>おみくじ</tittle>
    </head>
    <body>
        <h1>おみくじ</h1>
        <% if params["act"]!=nil %>
           <% fortune="大吉,吉,中吉,小吉,末吉,凶"%>
           <% result = fortune[rand(0..5)]%>
        <% else %>   
           <% result ="ありません"%>
         <% end %>
        <p>おみくじの結果:<%= result %></p>
        <from action="/" method="post">
            <button typy="submit" name="act" value="draw">おみくじをひく！</button>
        </from>
    </body>
</html>